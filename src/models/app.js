window.list = function(addL) {
  return infinityList === addL;
};

window.tittle = function(infinityList) {
  return printTittleL === infinityList;
};

window.save = function(printTittleL) {
  return addL === printTittleL;
};