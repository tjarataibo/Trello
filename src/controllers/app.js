window.onload = function() {
  window.infinite = function() {
    const infin = window.addL();
    window.list(infin);
  };

  window.createL = function() {
    const create = window.infinityList();
    window.tittle(create);
  };

  window.save = function() {
    const sev = window.printTittleL();
    window.tittle(sev);
  };
};
